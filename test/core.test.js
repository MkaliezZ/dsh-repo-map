import test from 'node:test'; import assert from 'node:assert/strict'; import {mkdtemp,writeFile,mkdir,rm} from 'node:fs/promises'; import {tmpdir} from 'node:os'; import path from 'node:path'; import {buildRepoMap} from '../src/core.js';
test('maps files and symbols deterministically',async()=>{const d=await mkdtemp(path.join(tmpdir(),'rm-')); await mkdir(path.join(d,'src')); await writeFile(path.join(d,'src/a.js'),'export function hello(){}'); const m=await buildRepoMap(d); assert.equal(m.files[0].path,'src/a.js'); assert.deepEqual(m.files[0].symbols,['hello']); await rm(d,{recursive:true});});
